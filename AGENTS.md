<!-- BEGIN:nextjs-agent-rules -->
sasasas
# This Is Not the Next.js You Know

This version has breaking changes. APIs, conventions, and file structure may all differ from your training data. Read the relevant guide in `node_modules/next/dist/docs/` before writing any code. The `next` package may not be visible from the repository root in a monorepo. Use the path resolved from this file's directory. Follow all deprecation notices.

`next dev` writes and re-adds this block. Removing it from a diff only recreates the uncommitted change. Commit it with your work to keep the tree clean.

<!-- END:nextjs-agent-rules -->
